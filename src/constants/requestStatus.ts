/**
 * Visualizing request status types
 */
export const enum RequestStatusType {
    started = "started",
    inProgress = "inProgress",
    completed = "completed",
    failed = "failed",
    cancelled = "cancelled"
}
