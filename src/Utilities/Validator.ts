class Validator {

    /**
     * Validates user settings
     * @param themeColor Theme color
     * @returns If it is valid then true else false
     */
    public static validateUserSettings()
    {
        // #region Theme Color
        
        // #endregion
    }
}

export {
    Validator
};